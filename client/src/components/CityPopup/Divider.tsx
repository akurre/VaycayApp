import { useAppStore } from '@/stores/useAppStore';
import { MapTheme } from '@/types/mapTypes';
import { appColors } from '@/theme';

const Divider = () => {
  return <div className="pt-3" style={{ borderTop: `1px solid` }} />;
};

export default Divider;
